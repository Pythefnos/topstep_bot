"""
Test package initializer.

Having this file lets pytest treat `tests/` as a package so you can use
relative imports inside tests if desired, e.g.:

    from src.risk.risk_manager import RiskManager
"""
# Nothing else required here.
